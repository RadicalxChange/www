---
layout: layouts/_base.njk
pagination:
  data: team
  size: 1
  alias: member
permalink: "team/{{ member.name | slug }}/"
defaultBio: |
  _No biography available at this time._
---

<div class="w-full h-full grid-cols-4 lg:grid-cols-12">
  <div
    class="team_image"
    style="background-image: url('/images/team/{{ member.image }}');"
  ></div>
  <div class="team_bio">
    <div class="md-container">
      <!-- prettier-ignore -->
      {% if member.bio %}
      {{ member.bio | markdown | safe }}
      <!-- prettier-ignore -->
      {% else %}
      {{ defaultBio | markdown | safe }}
      <!-- prettier-ignore -->
      {% endif %}
    </div>
  </div>
</div>
