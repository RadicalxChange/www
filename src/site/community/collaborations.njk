---
layout: layouts/_base.njk
title: "Collaborations"
---

<!-- prettier-ignore -->
{% import "components/close-button.njk" as closeButton %}

<!-- prettier-ignore -->
{{ closeButton.render("/community") }}
<section
  id="jobBoard"
  class="grid grid-cols-layout-4 lg:grid-cols-layout-12 pb-4 lg:pb-8 my-margin"
>
  <header class="col-span-columns lg:col-start-column-1 lg:col-end-gutter-8 mb-line-2 lg:mb-0">
    <h1 class="font-display text-size-display break-words lg:text-size-lg/display uppercase"
      >Collaborations</h1
    >
  </header>
  <ol class="col-span-full mt-12">
    <!-- prettier-ignore -->
    {% for s in collaborations %}
    <div class="grid grid-rows-auto/1fr grid-cols-layout-4 lg:grid-cols-layout-12">
      <button type="button" class="collapsible text-left focus:outline-none text-size--1 col-span-full grid grid-cols-layout-4 lg:grid-cols-layout-12 my-4">
        <div class="col-start-column-1 col-end-column-3 lg:col-start-column-1 lg:col-end-gutter-8">
          <h1 class="uppercase break-words">{{ s.title }}</h1>
          <h2>{{ s.organization }}</h2>
        </div>
        <div class="text-right col-start-column-3 col-end-margin-2 lg:col-start-column-9 lg:col-end-margin-2">
          <p>{{ s.location }}</p>
          <p>{{ s.datePosted }}</p>
        </div>
      </button>
      <div class="hidden row-start-2 col-span-full grid grid-cols-layout-4 lg:grid-cols-layout-12 my-4 pb-12 lg:pb-16">
        <div class="col-span-columns lg:col-start-column-1 lg:col-end-gutter-8">
          <div class="markdown markdown-sm mb-12">
            {{ s.description | markdown | safe }}
          </div>
          <a class="thick-link text-size--1 uppercase" href="{{ s.applicationLink }}">
            Apply for this job
          </a>
        </div>
      </div>
      <div class="col-span-columns lg:border-b"></div>
    </div>
    <!-- prettier-ignore -->
    {% endfor %}
  </ol>
</section>
<script>
  var collapsibles = document.getElementsByClassName("collapsible");
  var i;

  for (i = 0; i < collapsibles.length; i++) {
    collapsibles[i].addEventListener("click", function() {
      this.classList.toggle("active");
      var content = this.nextElementSibling;
      if (content.style.display === "grid") {
        content.style.display = "none";
      } else {
        content.style.display = "grid";
      }
    });
  }
</script>
<script>
  // prettier-ignore
  {% include "components/close-button.js" %}
</script>
